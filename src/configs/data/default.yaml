# @package _global_
dataset:
  # Train split: used by train.py with split_train_val (hold-out val for training curves).
  train_dir: /Data/nadine.hage-chehade/train
  # Full validation split: used by evaluate.py (no random split; entire folder).
  val_dir: /Data/nadine.hage-chehade/val
  # Test frames root for create_submission.py
  test_dir: /Data/nadine.hage-chehade/test
  submission_output: ~/submission.csv

  num_frames: 4
  val_ratio: 0.1
  seed: 42
  max_samples: null
